#include <stdio.h>
#include <stdlib.h>

int main(){
  /*
  for(int i = 1; i <= 9; i++){
    for(int j = 2; j <= 9; j++ ){
      printf("%d X %d = %d\n", i, j, i*j);
    }
  }
  
  
  //아스키코드로 문자 출력
  for(int i = 0; i<('Z' - 'A' + 1); i++){
    printf("%c", 'A' + i);
  }
  
  
  //소수 구하기
  //2부터 50까지의소수 출력
  for(int i=2; i <= 50; i++){
    int j = 0;
    for(int j = 2; j < i; j++){
      if(i%j == 0){
        printf("%d",i);
        break;
      }
    }
    if(i==j){
      printf("%d",i);
    }
  } 
  
  int sum = 0;
  int count = 0;
  while(sum < 300){
    sum += count;
    count++;
    }
  printf("sum = %d\n", sum);
  printf("last number = %d\n", count);
  */
  
  //do - while
  
  return 0;
}